<tr class="patient-box">
  <td><%= patient.id %></td>
  <td><%= link_to patient.full_name, patient %></td>
  <td><%= patient.age %></td>
  <td><%= patient.created_at.strftime('%Y/%m/%d') %></td>
</tr>
