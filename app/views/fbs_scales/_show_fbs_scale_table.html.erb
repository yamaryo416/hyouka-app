<table class="table">
  <thead>
    <tr>
      <th scope="col">項目</th>
      <th scope="col">評価結果</th>
    </tr>
  </thead>
  <tbody>
    <% fbs_scale.scale_attributes.each_with_index do |attr_name, i| %>
      <tr>
        <td class="<%= attr_name %>">
          <span><%= i +1 %>)</span>
          <%= FbsScale.human_attribute_name(attr_name) %></td>
        <td class="<%= attr_name %>_score"><%= fbs_scale.send("#{attr_name}_i18n") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
